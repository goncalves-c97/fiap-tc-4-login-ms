apiVersion: v1
kind: Service
metadata:
  name: login-ms-webapi-service
  namespace: login-ms
spec:
  type: LoadBalancer
  selector:
    app: login-ms-webapi
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5000
